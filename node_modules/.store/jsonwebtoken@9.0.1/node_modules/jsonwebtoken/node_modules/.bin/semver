#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/E:/Web_object/自己练习node后端项目/yujie-node-test/node_modules/.store/semver@7.5.4/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/E:/Web_object/自己练习node后端项目/yujie-node-test/node_modules/.store/semver@7.5.4/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../semver@7.5.4/node_modules/semver/bin/semver.js" "$@"
else
  exec node  "$basedir/../../../../../semver@7.5.4/node_modules/semver/bin/semver.js" "$@"
fi
